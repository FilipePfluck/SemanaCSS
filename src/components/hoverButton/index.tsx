import React from 'react'

import * as S from './styles'

const HoverButton: React.FC = () => {
    return (
        <S.Container>
            <button>Passe o mouse por cima</button>
        </S.Container>
    )
}

export default HoverButton